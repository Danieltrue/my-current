import styled from "styled-components"

const Learnstyle = styled.section`
  min-height: 500px;
  width: 100%;
  background-color: var(--c5);
  margin: 50px 0;
  .row {
    padding: 3rem 1rem;
    h4 {
      color: var(--font-color);
      font-family: var(--serif);
      font-size: 140%;
    }
    main {
      margin-top: 40px;
    }
  }
`

export default Learnstyle
